/**
 * Constantes du module Uniformes sans dépendance serveur : importables depuis
 * un composant client sans embarquer `@/lib/db`.
 */

/** Plafond de lignes de l'onglet « Emprunts » (les plus récentes). */
export const UL_LOANS_LIMIT = 500;
